# Wasteland Survivor 美术生成踩坑与经验总结

> 这份文档总结了这次为 **Wasteland Survivor** 做 **地图 Tileset、Tileset 拼接、角色、怪物** 美术资源时踩过的坑，以及后续可复用的经验。
>
> 目标不是写概念，而是把真正影响效率、质量、落地的点记下来，方便以后少走弯路。

---

## 1. 项目背景

- 项目：`Wasteland Survivor`
- 引擎：Godot 4.6
- 类型：俯视角 2D Roguelike / Vampire Survivors Like
- 平台倾向：Web 优先
- 美术方向：废土 / 后启示录 / 像素风 / low top-down

这次的主要工作是：

1. 梳理项目实际需要的美术资源
2. 用 PixelLab 批量生成地图、角色、怪物、拾取物
3. 将资源下载、整理、落地到 Godot 项目中
4. 调整引擎内显示尺寸与组织结构

---

## 2. 总体经验：先盘需求，再批量生成

### 不要一上来就生成图

最容易浪费次数、浪费时间的方式，是“想到一个资源就生成一个”。

这次比较正确的做法是：

- 先读项目代码和配置
- 明确有哪些资源类型
- 明确每类资源的数量、用途、尺寸、视角
- 再统一批量生成

### 这次最终整理出的资源分类

- **玩家角色**：4 个
- **敌人**：6 个
- **地图装饰物**：5 个
- **拾取物**：3 个
- **地面 Tileset**：16 张 Wang tile

### 为什么这一步很重要

因为不同资源对应的 API 端点完全不同：

- 角色：`/create-character-with-4-directions`
- 地图物件：`/map-objects`
- Tileset：`/tilesets`

如果前面分类不清，后面脚本会越来越乱，下载逻辑也会混在一起。

---

## 3. 地图 Tileset 设计经验

## 3.1 Tileset 不是“随便出几张地砖”

Tileset 最重要的不是单张图好不好看，而是：

- 能不能无缝拼
- 明暗/色相是否统一
- 边缘逻辑是否一致
- 在游戏里连续铺开时是否稳定

这次的结论是：

> **先保证拼接逻辑成立，再追求单张 tile 的细节。**

单张 tile 如果很花，但拼起来边界乱、噪点乱、方向不统一，落地后会比简单 tile 更难看。

---

## 3.2 适合当前项目的 Tileset 方向

这次比较适合项目的是：

- 俯视角（`low top-down`）
- 废土地表 / 沙土 / 裂纹 / 金属碎片感
- 32×32 尺寸
- 低饱和、偏灰褐色
- 单 tile 内不要塞太多高对比信息

原因：

- 游戏本身节奏快，玩家不会长时间盯一块地砖
- 画面里还会叠加敌人、投射物、掉落物、UI 提示
- 地面太抢眼，会直接干扰玩法可读性

### 经验

**地图底层素材宁可“稳”、不要“炸”。**

也就是：

- 可重复
- 不刺眼
- 不抢主体
- 一眼能看出地面是同一套世界观

---

## 4. Tileset 拼接经验

## 4.1 Tileset 下载结果不是直接可用的“大图”

这次一个非常实际的点是：

PixelLab 返回的 tileset 结果，不是传统意义上一张完整 tilesheet，而是：

- 通过 `/tilesets/{id}` 获取 tileset 数据
- 从 `tileset.tiles[].image.base64` 里逐张取 tile
- 再保存成单张 PNG

也就是说，这次脚本拿到的是：

- `tile_xxx.png`
- 每张 tile 独立保存

而不是一开始就拿到 Godot 能直接拖进去的成品图集。

### 这意味着什么？

意味着“生成成功”不等于“已经能直接在引擎里用”。

中间还差一层：

- 统一命名
- 确认尺寸一致
- 必要时二次拼接成图集
- 再在 Godot 里建 TileSet / Atlas / TileMap 规则

---

## 4.2 拼接 Tileset 时最该注意的不是技术，而是规则

如果后续要把单 tile 拼成 tilesheet，最重要的是：

1. **所有 tile 尺寸完全一致**
2. **透明边距一致**
3. **命名规则一致**
4. **编号顺序清晰**

否则会出现：

- 拼接出来的图集对不齐
- Godot Atlas 切片偏移
- 某些 tile 看起来模糊或边缘抖动
- 自动铺图时逻辑混乱

### 推荐做法

后续如果继续完善，建议固定成：

- `assets/sprites/tileset/tile_00.png`
- `assets/sprites/tileset/tile_01.png`
- ...
- 再额外生成一个说明文件，写清楚每张 tile 的用途

不要只保存随机 ID 命名版本，不然后面维护很痛苦。

---

## 4.3 Wang Tileset 的经验

这次生成的是 **16 张 Wang tile**，这是个很实用的起点。

优点：

- 足够做基础地面变化
- 数量不算大，容易整理
- 比纯随机地砖更容易形成“有规律但不死板”的地面

但要注意：

> Wang tile 能解决“拼接逻辑”，解决不了“艺术统一性”。

也就是说：

- 如果每张 tile 的材质语言不统一
- 裂纹方向乱
- 颜色偏差大
- 局部高亮太多

那即使技术上能拼，也会看起来很碎。

所以在 prompt 或描述上，要始终强调：

- 同一套世界观
- 同一材质家族
- 同一光照逻辑
- 同一像素密度

---

## 5. 角色设计经验

## 5.1 4 方向比 8 方向稳定得多

这是这次最明确的结论之一：

> **4 方向稳定，8 方向不稳定。**

实际遇到的问题：

- 8 方向角色生成时，偶发 `bone_scaling` 错误
- 4 方向角色整体成功率明显更高

### 实战建议

当前项目这种俯视角生存游戏，优先使用：

- **4 directions**
- 不要一开始就追求 8 directions

原因：

- 当前玩法不依赖特别精细的朝向表达
- 4 方向已经足够支撑大多数视觉反馈
- 能显著降低生成失败率和调试成本

### 结论

> 对这个项目来说，**4方向是性价比最高的方案**。

---

## 5.2 角色描述要先保证轮廓识别

这次角色效果好的关键，不在于“描述得多华丽”，而在于：

- silhouette 清楚
- 身份特征清楚
- 装备特征清楚
- 废土世界观统一

比如玩家角色需要首先做到：

- 一眼能区分 Survivor / Scavenger / Demolisher / Mutant
- 不用看名字，也能感受到职业差异

### 比“细节多”更重要的是“识别快”

因为实际游戏里：

- 角色尺寸不大
- 移动中观察时间很短
- 屏幕里信息密度高

如果角色依赖特别细的小装饰来区分，落到游戏里几乎等于没有。

---

## 5.3 角色生成后不要直接认定“能用”

这次落地时发现一个很现实的问题：

- 资源生成出来看着不错
- 但进引擎后，尺寸、站位、视觉重心不一定合适

比如拾取物 `xp_gem`：

- 图本身没问题
- 但放进游戏后视觉上显得偏大
- 最后在场景里把 `Sprite2D.scale` 改成了 `Vector2(0.5, 0.5)` 才更协调

### 经验

> 生成图只是第一步，真正“可用”要经过引擎内验证。

至少要检查：

- 显示尺寸
- 锚点/中心点
- 与碰撞范围是否一致
- 与其他素材摆在一起是否协调

---

## 6. 怪物设计经验

## 6.1 怪物的第一目标不是“帅”，而是“读得快”

怪物在这类游戏里要承担的职责是：

- 让玩家一眼看出危险来源
- 让玩家区分不同攻击/行为类型
- 在大量刷怪时保持可读性

所以怪物设计重点应该优先放在：

- 轮廓差异
- 体型差异
- 色块差异
- 运动趋势差异

而不是一味追求复杂纹理。

### 这次效果比较好的怪物

- **Boss Ash Behemoth**：体型、轮廓、发光裂纹特征都很强
- **Acid Bug**：昆虫感明确，绿色发光体很容易识别

这些都说明：

> 对敌人来说，**功能性的轮廓特征** 比“表面花纹细节”更重要。

---

## 6.2 敌人类型要与行为联动思考

设计怪物时不能只想“长什么样”，还要同步想：

- 它怎么移动
- 它是否远程
- 它是否冲锋
- 它是否爆炸
- 它是精英还是杂兵

比如：

- `exploder` 应该从外形上就带有“不稳定、危险、会爆”的感觉
- `acid_bug` 应该从配色和肢体结构上就有“有毒”的暗示
- `iron_giant` 应该天然��现厚重、迟缓、抗打

### 经验

**怪物美术如果不能辅助玩法判断，就只是装饰。**

---

## 6.3 Boss 值得单独给更高规格

这次 Boss Ash Behemoth 用更高规格处理后，效果明显更好。

原因很简单：

- Boss 出场频率低
- 屏幕占比大
- 玩家会盯着看更久
- 它值得更多细节和更强视觉特征

### 建议

普通敌人可以偏统一、偏工业化批量处理；
但 Boss 最好单独打磨：

- 更强 silhouette
- 更明显局部发光/特征部位
- 更高辨识度
- 与普通敌人明显拉开层级

---

## 7. 这次真正踩过的坑

## 7.1 并发限制：最多 3 个 background jobs

这是最大的真实坑之一。

### 现象

一口气提交太多任务时：

- 超过 3 个 background jobs 会直接 fail
- 看起来像“API 不稳定”
- 实际上是并发上限问题

### 正确做法

- 每批最多 3 个
- 等这批完成后再提下一批
- 用脚本统一轮询状态

### 经验

> 不要把“批量生成”理解成“全量一口气提交”。

在这个 API 里，批量的正确方式是：

- **小批次串行**
- 不是“大批次并行”

---

## 7.2 API 是异步的，不能提交完就立刻下载

这次另一个明显坑是：

- 提交成功 ≠ 图片已经准备好
- 要先拿 `background_job_id`
- 再轮询 `/background-jobs/{id}`
- 状态完成后才能取结果

### 正确心智模型

流程应该是：

1. submit
2. 记录 job id
3. poll
4. done / failed
5. download

不要写成：

1. submit
2. 直接下载

这种写法会不断打到空结果，误以为接口坏了。

---

## 7.3 不同资源类型，下载结构完全不同

这是这次脚本设计里最容易混掉的点。

### 角色

角色接口可以拿到：

- `character_id`
- `background_job_id`

完成后可以通过：

- `/characters/{id}/zip`

下载 zip，里面包含：

- 4 方向 PNG
- metadata

### 地图物件

地图物件通常要从 background job 返回结果里拿：

- `last_response.image.base64`

### Tileset

Tileset 要去：

- `/tilesets/{id}`
- 然后遍历 `tileset.tiles[].image.base64`

### 经验

> 三类资源不要共用一套“下载假设”。

必须按资源类型分别写下载逻辑。

---

## 7.4 macOS 默认 bash3，不支持关联数组

这次一开始写 bash 脚本时踩到了很经典的 macOS 坑：

- 默认 `/bin/bash` 太老
- 不支持 bash4 的关联数组能力

结果就是：

- 批量资源配置写起来很难受
- 容易出现兼容性问题
- 后续维护性也差

### 结论

> 这种稍复杂的批量任务，直接上 Python 更稳。

后面改成 Python 之后，提交、轮询、下载、分目录保存都明显顺畅很多。

---

## 7.5 下载后的目录归类可能出错

这次实际发生过：

- `walker`
- `mutant_dog`

被下载到了 `characters/` 目录，而不是 `enemies/`。

原因不是 API 本身出错，而是：

- 批处理归档逻辑和资源分类耦合得太死
- 同批提交时容易按错误分类落盘

### 经验

下载脚本一定要：

- 明确资源类别
- 明确目标目录
- 不要只依赖“这一批里大概是什么”来判断归属

否则后期整理成本会越来越高。

---

## 7.6 生成结果不一定完全符合像素风预期

这次像 **wrecked car（废车）** 就出现了一个问题：

- 主题是对的
- 但画风偏写实
- 不够像素化

这说明即便同一个模型、同一批生成：

- 有些素材会更贴近目标风格
- 有些素材会“跑偏”到更写实的方向

### 经验

不要默认“一次生成整套就都能用”。

应该接受这件事：

- 80% 可用已经很好
- 剩下 20% 需要重生、筛选、后处理

---

## 7.7 引擎内尺寸和碰撞经常与视觉尺寸脱节

这次 `xp_gem` 是典型案例：

- 图替换了
- 视觉尺寸变了
- 但引擎内的拾取判定还是原来的

### 这类问题非常常见

任何替换占位图为正式美术后，都应该检查：

- Sprite scale
- 碰撞体大小
- 交互范围
- 排序层级
- 阴影 / 发光 / shader 是否还适配

### 经验

> 美术替换从来不是“只换 PNG”。

它通常至少是：

- PNG + Scene 调整
- PNG + Scale 调整
- PNG + Collision 调整
- PNG + 逻辑验证

---

## 7.8 发布到 GitHub 前一定要扫 Token

这次仓库里真实存在过：

- PixelLab API token 被硬编码在多个脚本里

如果直接推公开仓库，就是实打实的泄漏。

### 已经验证过的正确做法

发布前先扫：

- `token`
- `secret`
- `api_key`
- `Authorization: Bearer`
- 已知 token 字面值

并且把脚本改成：

- 走环境变量读取
- 不把 secret 写死在代码里

### 结论

> 只要项目要上 GitHub，这一步必须是固定流程，不是可选项。

---

## 8. 推荐工作流（以后照这个走）

## 第一步：先盘资源需求

先列清单：

- 玩家角色
- 怪物
- Boss
- 地图装饰物
- 拾取物
- 地砖 / Tileset
- UI 图标（如果需要）

不要先生成，再回头想少了什么。

---

## 第二步：按资源类型拆任务

至少拆成：

- characters
- enemies
- objects
- pickups
- tileset

每类有自己的：

- prompt 规则
- 尺寸规则
- 下载规则
- 落盘目录

---

## 第三步：分批提交

统一原则：

- 每批最多 3 个 job
- 记录每个 job id
- 完成后再提下一批

不要为了快，把整个批处理打爆。

---

## 第四步：下载后立刻做目录清洗

下载完马上整理：

- 统一命名
- 统一目录
- 清理错分类
- 去掉测试废图

否则后面一旦和 Godot 场景、脚本开始绑定，就会越来越难改。

---

## 第五步：进引擎做真实验证

重点看：

- 尺寸是否协调
- 动线是否清楚
- 主体是否可读
- 掉落物是否抢戏
- 地面是否太花
- 怪物是否一眼能区分

在图片查看器里好看，不等于在实机里好看。

---

## 第六步：准备公开仓库时做安全清理

固定检查：

- token / secret
- 本机绝对路径
- 临时脚本
- 生成缓存目录
- `.godot`
- 不该上传的 build 或证书

---

## 9. 最终结论

这次最有价值的经验可以浓缩成下面几句：

1. **先整理需求，再批量生成。**
2. **4方向角色比 8 方向稳定得多。**
3. **Tileset 重点是拼接一致性，不是单张花哨。**
4. **怪物优先保证可读性，不要只追求酷。**
5. **PixelLab 批处理必须控制并发（最多 3 个 job）。**
6. **不同资源类型的下载逻辑必须分开写。**
7. **正式落地一定要进 Godot 检查尺寸、碰撞和显示效果。**
8. **上 GitHub 前必须扫 secret，不能把 token 留在脚本里。**

---

## 10. 后续建议

如果继续做下一轮迭代，我建议优先补这几件事：

### A. Tileset 规范化
- 把 tile 命名改成稳定顺序
- 补一份 tile 对照说明
- 必要时拼成统一图集

### B. 角色/敌人动画化
- 在现有静态方向图基础上，继续补 walk / attack / idle
- 不要一开始就全角色全动作铺开，先验证主角 + 1~2 个敌人

### C. 视觉尺寸统一
- 系统性检查角色、敌人、掉落物、障碍物的显示比例
- 不要逐个靠感觉修

### D. 资产落地规范
- 每类资源目录固定
- 命名固定
- 生成脚本固定
- 安全检查固定

---

如果以后还继续用 PixelLab 或类似工具，这份文档可以直接当作第一版生产规范。

---

## 11. 新增经验：Web 端 UI 文本“乱码/方框”排障（2026-04-23）

### 11.1 现象
在 Godot Web 导出页面里，中文正常，但部分英文注释行出现“方框/乱码”，典型表现为：

- `game code falls back...` 这类纯英文句子里，字母间出现方框
- 中文 UI 标题、正文基本正常

### 11.2 根因
这次根因不是编码（UTF-8），而是**字体子集缺字**：

1. 早期 `tools/subset_fonts.py` 的采集逻辑偏向“中文字符”
2. 子集字符表没有稳定覆盖基础 ASCII（尤其是空格）
3. 后续又基于已 subset 的字体继续 subset，导致字形集越来越“窄”

结果：页面文本里一旦出现未包含的字形（特别是空格/基础英文符号），就会渲染为方框。

### 11.3 本次修复动作

1. **重建子集字符集**：
   - 全量扫描 `src/**/*.gd`、`src/**/*.tscn`、`assets/**/*.json`、`assets/**/*.tscn`、`project.godot` 等文本来源
   - 强制加入基础 ASCII `U+0020..U+007E`
   - 额外加入常用空白/符号（`U+00A0`、`U+3000`、项目常见符号）

2. **修订 `tools/subset_fonts.py`（防回归）**：
   - 不再只收集 `ord(ch)>127` 字符
   - 默认优先使用 full font 作为 subset 源（支持 `assets/fonts/full/*.ttf` 或 `/tmp/*-full.ttf`）
   - subset 后执行必需字形校验（至少含空格、斜杠、大小写字母等）

3. **强制重导入 + 重导出**：
   - 清理 `.godot/imported/PingFangSC-*.fontdata`
   - 重新执行 Web 导出，确保新字形进入产物

### 11.4 预防方案（建议固定流程）

#### 方案 A（推荐，稳定优先）
开发期直接使用完整字体（不 subset）；只在发布阶段 subset。

- 优点：几乎杜绝“新增文案就乱码”
- 缺点：包体稍大

#### 方案 B（继续 subset，但流程化）
每次新增 UI 页面/文案后，发布前统一执行：

1) `python3 tools/subset_fonts.py`
2) 删除对应 `.godot/imported/*.fontdata`
3) 重新导出 Web

并在 CI/发布脚本增加“字形校验失败即中断”。

### 11.5 结论
这类问题本质是**字体资产管线问题**，不是业务代码问题。

只要把“字符采集 + full font 源 + 必需字形校验 + 重导出”做成固定流水线，后续新增页面不会再反复踩坑。
